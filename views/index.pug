extends layout

block content
  h1= title
  p Post a blog

  form(method="post", action="/insert_post")
    input(name="title")
    textarea(name="body")
    button(type="submit") Submit


    each allPosts in returnedAllPosts
      p= allPosts.title
      p= allPosts.body
